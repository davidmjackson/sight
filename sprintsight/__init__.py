"""Sprintsight — AI delivery-intelligence layer.

Stage 1 (Ingestion + RAG Core). Only the eval harness lives here so far; ingestion,
retrieval, and the detector land in later Stage-1 stories.
"""

__version__ = "0.0.0"
