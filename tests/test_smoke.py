"""Placeholder smoke test so the CI skeleton is green before real tests exist.

Eval-first (CLAUDE.md): real test and eval code lands in Stage 1, never before the
eval a feature must pass exists. This only proves the lint + test harness is wired.
"""


def test_harness_is_wired():
    assert True
